export interface alt { alt: string; }
export interface autocomplete { autocomplete: string; }
export interface cite { cite: string; }
export interface disabled { disabled: boolean; }
export interface download { download: string; }
export interface href { href: string; }
export interface name { name: string; }
export interface open { open: boolean; }
export interface rel { rel: string; }
export interface src { src: string; }
export interface referrerPolicy { referrerPolicy: string; }
export interface target { target: string; }
export interface text { text: string; }
export interface type { type: string; }
export interface value { value: string; }
export interface dimensions {
  height?: string;
  width?: string;
}